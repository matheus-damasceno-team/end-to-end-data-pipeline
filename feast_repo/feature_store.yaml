# Feast Feature Store Configuration
# PATH: feast_repo/feature_store.yaml

project: feast_repo
registry: s3://feast-registry/registry.db
provider: local
online_store:
  type: redis
  connection_string: redis:6379
offline_store:
  type: file