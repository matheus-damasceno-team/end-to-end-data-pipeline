# This file is intentionally left empty.
# It's used to ensure that the 'airflow/plugins' directory is tracked by Git,
# as Git does not track empty directories.
# Custom Airflow plugins (hooks, operators, sensors, etc.) can be placed here.
